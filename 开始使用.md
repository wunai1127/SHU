# 🚀 心脏移植AI决策系统 - 快速开始

## 📌 您现在有什么

✅ **完整的知识图谱构建系统**（`automated_kg_pipeline/`）
- 全自动流水线：从20000+篇文章 → Neo4j知识图谱
- 支持API（DeepSeek/OpenAI）或本地LLM
- 断点续传、质量监控、自动优化

✅ **中文医学Schema**（`schemas/`）
- 11种实体类型 + 10种关系类型
- 基于7个官方心脏移植指南设计
- 支持证据强度追踪和统计量

✅ **Graph RAG Agent集成方案**（`automated_kg_pipeline/INTEGRATION_STRATEGY.md`）
- GraphRetriever：多跳推理、路径查询
- EvidenceTracker：证据去重
- WorkerCoordinator：Multi-Agent协调

---

## ⚡ 3步开始构建知识图谱

### 第1步：配置系统（5分钟）

```bash
cd /home/user/SHU/automated_kg_pipeline

# 编辑配置文件，填写3处信息
vim config.yaml
```

**需要填写的3处**：
1. DeepSeek API密钥（获取：https://platform.deepseek.com/）
2. Neo4j密码（访问 http://localhost:7474 设置）
3. 数据路径（如果字段名不同，修改`field_mapping`）

**详细指南**：阅读 `配置指南.md`

---

### 第2步：上传数据（5分钟）

将您的20000+篇医学文章JSON文件放到：

```bash
/home/user/SHU/data/medical_abstracts/
```

**数据格式示例**：参考 `data/medical_abstracts/sample_format.json`

```json
[
  {
    "pmid": "12345678",
    "abstract": "Background: This study evaluated...",
    "title": "...",
    "year": 2023
  }
]
```

---

### 第3步：运行系统（1分钟）

```bash
cd /home/user/SHU/automated_kg_pipeline

# 方式1: 快速启动脚本（推荐）
./快速启动.sh

# 方式2: 先测试10篇文章
python test_system_ready.py --config config.yaml
python auto_kg_builder.py --config config.yaml --test-mode --max-articles 10

# 方式3: 全量运行
python auto_kg_builder.py --config config.yaml
```

**预计时间**：
- DeepSeek API: 30-60分钟（成本~50元）
- 本地4 GPU: 约14小时

---

## 📂 文件导航

### 🔧 核心系统

| 文件 | 说明 |
|------|------|
| `automated_kg_pipeline/auto_kg_builder.py` | 主流水线脚本 |
| `automated_kg_pipeline/config.yaml` | **配置文件（需要修改）** |
| `automated_kg_pipeline/快速启动.sh` | 一键启动脚本 |
| `automated_kg_pipeline/test_system_ready.py` | 系统检查工具 |

### 📖 用户文档

| 文件 | 说明 |
|------|------|
| `automated_kg_pipeline/配置指南.md` | ⭐ **配置详细说明** |
| `automated_kg_pipeline/配置检查清单.txt` | 配置前逐项检查 |
| `automated_kg_pipeline/README.md` | 完整系统文档 |

### 📊 技术文档

| 文件 | 说明 |
|------|------|
| `automated_kg_pipeline/OPTIMIZATION_STRATEGY.md` | 性能优化策略（4x加速） |
| `automated_kg_pipeline/INTEGRATION_STRATEGY.md` | Graph RAG Agent集成 |
| `UPLOADED_RESOURCES_ANALYSIS.md` | 7个中文指南分析报告 |

### 🗂️ 数据与Schema

| 文件 | 说明 |
|------|------|
| `schemas/chinese_medical_kg_schema.json` | 医学KG Schema |
| `schemas/medical_synonyms.json` | 医学术语同义词库 |
| `data/medical_abstracts/sample_format.json` | 数据格式示例 |
| `data_txt/完整 txt/` | 7个中文心脏移植指南 |

---

## ✅ 配置检查清单

运行前请确认：

- [ ] 已填写 `config.yaml` 中的 API 密钥
- [ ] 已填写 `config.yaml` 中的 Neo4j 密码
- [ ] 已上传20000+篇文章到 `data/medical_abstracts/`
- [ ] Neo4j已启动（`sudo systemctl status neo4j`）
- [ ] 已安装依赖（`pip install -r requirements.txt`）
- [ ] 已通过系统检查（`python test_system_ready.py`）

**详细清单**：`配置检查清单.txt`

---

## 🎯 预期输出

运行完成后，您将得到：

### Neo4j 知识图谱
- **~450,000 实体节点**（供体特征、受体特征、风险因子、并发症等）
- **~780,000 关系边**（导致、增加风险、缓解等）

### 质量报告
- `output/final/build_report.json`：统计摘要
- `validation/manual_review_samples.json`：100个人工验证样本
- `logs/kg_construction.log`：完整运行日志

### 备份文件
- `output/final/entities.csv`
- `output/final/relations.csv`

---

## 🆘 遇到问题？

### 常见问题快速解决

| 问题 | 解决方法 |
|------|---------|
| "配置文件格式错误" | 检查YAML缩进（必须用空格，不能用Tab） |
| "数据目录为空" | 确认文章已上传到 `data/medical_abstracts/` |
| "Neo4j连接失败" | 运行 `sudo systemctl start neo4j` |
| "LLM API密钥无效" | 检查 `config.yaml` 中密钥是否正确 |
| "CUDA out of memory" | 降低 `batch_size`（从8改为4） |

**详细排查**：参考 `配置指南.md` 第6节

---

## 📞 获取帮助

1. **快速配置**：阅读 `配置指南.md`
2. **系统文档**：阅读 `automated_kg_pipeline/README.md`
3. **性能问题**：阅读 `OPTIMIZATION_STRATEGY.md`
4. **集成方案**：阅读 `INTEGRATION_STRATEGY.md`

---

## 🔄 下一步

构建完成后：

1. **验证KG质量**
   ```cypher
   // 在 Neo4j Browser (http://localhost:7474) 中查询
   MATCH (n) RETURN count(n)  // 查看节点总数
   MATCH ()-[r]->() RETURN type(r), count(*) ORDER BY count(*) DESC  // 关系分布
   ```

2. **集成Multi-Agent系统**
   - 实现 RAG Agent（使用GraphRetriever）
   - 实现 Medical Expert Agent
   - 实现 Analyzer Agent（GNN+LLM）

3. **训练GNN模型**
   - 使用构建的KG训练风险预测模型
   - 实现物理约束的风险量化

---

## 📊 系统架构

```
输入: 20000+篇医学文献
  ↓
[AutoSchemaKG Pipeline]
  ├─ LLM抽取三元组
  ├─ Schema验证
  └─ 实体归一化
  ↓
[Neo4j Knowledge Graph]
  ↓
[Multi-Agent System]
  ├─ Medical Expert Agent
  ├─ RAG Agent
  └─ Analyzer Agent
  ↓
输出: 移植决策 + 风险评分 + 可解释性
```

---

**开始构建您的心脏移植AI系统！** 🚀
